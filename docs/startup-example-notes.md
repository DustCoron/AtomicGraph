# Startup Example Notes

- Status: stable
- Confirmed by user on 2026-03-08

This repository currently uses the startup graph defined in `src/App.tsx` under `INIT_DATA` as the approved stable startup example.

Guideline:
- Do not replace this startup example with another demo/example unless explicitly requested by the user.
- If changes are needed, update this note with date and reason.
